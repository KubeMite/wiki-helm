# Wiki Helm Chart

This repository contains the Helm chart responsible for deploying the [wiki](https://github.com/KubeMite/wiki) website Docker image into the Kubernetes cluster.

## Installation

To add the chart repository and deploy the latest version, run the following commands:

```sh
helm repo add wiki https://kubemite.github.io/wiki-helm
helm repo update
```
